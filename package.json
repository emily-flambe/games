{
  "name": "games-platform",
  "version": "1.0.0",
  "description": "Minimal games platform on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}